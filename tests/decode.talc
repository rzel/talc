#!/usr/bin/talc

words := "68 65 6c 6c 6f    20    77 6f 72 6c 64 0a".split(" +");
result := "";
# This is deliberately verbose to test more of the compiler!
for (i := 0; i < words.size(); ++i) {
  word := words[i];
  result += ("0x" + word).to_i().to_char();
}
print(result);
