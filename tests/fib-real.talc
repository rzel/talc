#!/usr/bin/talc

function real fib(n:real) {
  if (n == 0.0 || n == 1.0) {
    return n;
  } else {
    return fib(n-1.0) + fib(n-2.0);
  }
}

for (i := 0.0; i < 36.0; ++i) {
  puts("n=", i, " => ", fib(i));
}
