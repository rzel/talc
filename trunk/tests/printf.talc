// This throws a MissingFormatArgumentException if we accidentally interpret
// the printf format string twice, which was an actual mistake in the first
// implementation of Talc's printf.
printf("%s", "%s");
