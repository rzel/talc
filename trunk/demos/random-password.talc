#!/usr/bin/talc

# FIXME: let the user override these on the command-line.
desiredLength: final = 10;
alphabet: final = "0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ";

s := "";
for (i := 0; i < desiredLength; ++i) {
    s += alphabet[rnd(alphabet.size())];
}
puts(s);
